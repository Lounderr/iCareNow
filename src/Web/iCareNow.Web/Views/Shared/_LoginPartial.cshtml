@using iCareNow.Common
@using iCareNow.Data.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (this.SignInManager.IsSignedIn(this.User))
{
    <a class="account-btn-container" asp-area="Identity" asp-page="/Account/Manage/Index"><span class="account-name">@Model.User?.FirstName @Model.User?.LastName</span><img class="profile-btn" src="/assets/profileBtn.svg" alt="" /></a>
}
else
{
    <a class="account-btn-container" asp-area="Identity" asp-page="/Account/Login"><img class="profile-btn" src="/assets/profileBtn.svg" alt="" /></a>
}
